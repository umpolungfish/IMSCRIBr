Gate 2 was vacuous: vessel_capacity checked n_ops <= 16 against a generator
that only ever draws n_ops from randint(3, 16), and vessel_fused/vessel_split
were both hardcoded True with no reachable False path. Also found the
volatile check was comparing K's distillation value against grammar['P'],
the wrong axis entirely, so it could never fire.

Ran the real banked instrument on both a frame-before-deposit word and a
deposit-before-any-frame word to confirm the underlying vessel-closure
concept is genuinely decidable ("open the region that HOLDS the result
before the region that COMPUTES it, and close them in that order") — the
fix is to the implementation, not the concept.

Added the twenty balneological heap folios (f75r-f84v) from ENGINE.md, each
carrying its own FSPLIT and FFUSE capacity. heap_folio now gets selected the
way ENGINE.md actually specifies, keyed off the pharmacy address's own folio
number mod 20, and both checks compare against that folio's real numbers
instead of a coincidence. Volatility is now K=distillation or (K=cold
maceration with a volatile step), reading the volatilis field that was
already generated per entry but never passed into gate context. Cold
maceration entries with a volatile step get the closure constraint exempted,
per ENGINE.md's own text.

Checked empirically: 2000 runs now split roughly 53% PASS, 34% AMBIGUOUS,
12% FAIL on Gate 2 alone, each with a real folio and a real number attached
to the reason. It was constant PASS before.
